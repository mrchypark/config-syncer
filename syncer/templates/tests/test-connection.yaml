apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "syncer.fullname" . }}-test-connection"
  labels:
    {{- include "syncer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "syncer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
